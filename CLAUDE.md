<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# CLAUDE.md

This file exists because Claude Code auto-discovers it. The actual brief for any AI agent in this repo — Claude, Codex, Copilot, Cursor, Aider, all of them — is **[`AGENTS.md`](AGENTS.md)** at the repo root. Read that first; it's the same content for every tool.

This file is intentionally minimal. If a Claude-Code-only convention ever becomes load-bearing (e.g. a slash-command pattern that doesn't exist in any other tool), capture it below this header — until then, this file is just a pointer so Claude finds the brief at its conventional location.
